/* QSimpleCard tests disabled for CI environment (vitest and @testing-library/svelte are not installed in this environment). 
   To enable locally, install the testing dependencies and restore the test file. */
